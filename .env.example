# Server configuration
PORT=5000
JWT_SECRET=supersecretjwtkey-change-in-production

# Custom SMTP Email Server Settings (Optional)
# If left blank, the application will automatically spin up an Ethereal Email test account and print preview URLs in the console logs.
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
